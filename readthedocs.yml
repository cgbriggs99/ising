# readthedocs.yml

version: 2

build:
  image: latest

python:
  version: 3.8
  install:
    - method: pip
      path: .

sphinx:
  builder: html
  configuration: source_docs/conf.py
  

conda:
  environment: source_docs/requirements.yaml